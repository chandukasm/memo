import 'package:flutter/material.dart';

class CardDevider extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      // width: 70,
      child: Divider(
        color: Colors.white,
      ),
    );
  }
}

const iconColor_inactive = Colors.white54;
const iconColor_active = Colors.greenAccent;
